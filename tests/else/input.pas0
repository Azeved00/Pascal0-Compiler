(* The dangling else thing *)
program delse;
var s : integer;
    n : integer;
    z : string;
begin
  s := readint();
  n := 1;
  if n>0 then
    if s>0 then z := 's' else z := '1';
  writestr('hi:');
  writestr(z)
end.
